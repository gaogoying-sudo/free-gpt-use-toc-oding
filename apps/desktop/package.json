{
  "name": "mac-gpt-codex-bridge-v0",
  "version": "0.1.0",
  "private": true,
  "description": "Mac local GPT ↔ Codex automation bridge V0",
  "main": "dist/main/index.js",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "start": "npm run build && env -u ELECTRON_RUN_AS_NODE electron .",
    "dev": "npm run build && env -u ELECTRON_RUN_AS_NODE electron .",
    "diag": "npm run build && env -u ELECTRON_RUN_AS_NODE electron . --diag",
    "typecheck": "tsc -p tsconfig.json --noEmit"
  },
  "dependencies": {
    "pngjs": "^7.0.0"
  },
  "devDependencies": {
    "@types/node": "^22.15.0",
    "@types/pngjs": "^6.0.5",
    "electron": "^36.0.0",
    "typescript": "^5.8.3"
  }
}
